This is a port of Quake, originally created by webOS Internals. Updated in 2026 by codepoet and webOS Archive to add Bluetooth and USB Controller Support.

http://www.libsdl.org/projects/quake/

The source code is from http://www.libsdl.org/projects/quake/src/sdlquake-1.0.9.tar.gz


================================================================================
BUILDING AND PACKAGING
================================================================================

This tree produces TWO separate apps from the same source. They install side by
side and neither replaces the other:

  Quake HD    org.webosarchive.sdlquakehd   TouchPad only. OpenGL ES 1.1 on the
                                            Adreno 220 at the panel's native
                                            1024x768. This is the active one.
  Quake       org.webosinternals.sdlquake   The original software-rendered
                                            build for phones. Stable for years;
                                            it needs no changes.

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

1. The HP webOS PDK, installed at /opt/PalmPDK. The build uses the PDK's own
   CodeSourcery cross compiler:

     /opt/PalmPDK/arm-gcc/bin/arm-none-linux-gnueabi-gcc     (reports 4.3.3)

   *** Do NOT substitute a modern ARM toolchain. *** This is the single most
   expensive mistake available here. The TouchPad's glibc is 2.8 and stock PDK
   binaries need only GLIBC_2.4; a Linaro 4.9.4 build links libm against
   GLIBC_2.15 and the binary then loads on nothing at all:

     ./quakehd.bin: /lib/libm.so.6: version `GLIBC_2.15' not found

   The build prints the requirement when it finishes -- it must say GLIBC_2.4.
   Check any binary yourself with:  readelf -V <binary> | grep GLIBC

   The one quirk of this old gcc: -funroll-loops makes it segfault (an internal
   compiler error in host_cmd.c). Do not add it. The build scripts already fall
   back per-file from -O2 to -O1 to -O0 if a file trips the compiler.

2. The webOS SDK command line tools on your PATH -- palm-install and novacom,
   normally /usr/local/bin. Only needed to install, not to build.

3. Game data. id1/pak0.pak in this repo is the shareware pak, which is
   redistributable and enough to build and play episode 1. For the full game,
   drop the retail pak0.pak and pak1.pak into id1/ before packaging; the
   packager copies whatever is in that directory into the ipk. This matters
   because the app is jailed on device and can only read its OWN directory --
   it cannot reach a pak installed by the other package.

--------------------------------------------------------------------------------
BUILD AND PACKAGE -- QUAKE HD (the TouchPad build)
--------------------------------------------------------------------------------

Both scripts use paths relative to their own directory, so run them from there:

    cd build/webos
    ./build-webos-gl.sh          # -> fbuild/webos-gl/quakehd.bin
    ./package-hd.sh              # -> ../../ipks/org.webosarchive.sdlquakehd_<ver>_armv7.ipk

Then, with the TouchPad connected over USB and Developer Mode on:

    cd ../..
    palm-install ipks/org.webosarchive.sdlquakehd_1.1.1_armv7.ipk

That is the whole install, upgrade AND uninstall procedure. Reboot afterwards
only if the package changed something outside its own directory (the maintainer
scripts patch the jail config for controller support, so do reboot on a first
install). Do not hand-edit or delete anything under /var/palm/jail -- those
directories are full of read-only bind mounts into /media/cryptofs, and removing
them can take the whole cryptofs mount down with them.

The version comes from ONE place: build/webos/hd/appinfo.json. The build reads
it and passes it to the compiler as -DQUAKEHD_VERSION_RAW, the packager reads it
for the ipk filename, and the update check compares against it. Bump it there
and everything follows.

--------------------------------------------------------------------------------
BUILD AND PACKAGE -- THE ORIGINAL PHONE BUILD
--------------------------------------------------------------------------------

    cd build/webos
    ./build-webos.sh             # -> fbuild/webos/sdlquake.bin
    ./package-webos.sh           # -> ../../ipks/org.webosinternals.sdlquake_<ver>_armv7.ipk

Its version lives in the TOP-LEVEL appinfo.json, not the one under build/webos/hd.

--------------------------------------------------------------------------------
TWO TRAPS WORTH KNOWING BEFORE YOU CHANGE ANYTHING
--------------------------------------------------------------------------------

Never ship a metadata.json with the HD build. Gating the app with
{"version":1,"devices":[101]} puts it into phone compatibility mode, and the
compositor then hands it a 320x480 surface instead of the panel's 1024x768.
This was confirmed by A/B on the same binary, adding and removing only that
file. package-hd.sh deliberately does not create one.

If a change does not seem to take effect, confirm what is actually IN the
package before debugging the code. String literals survive stripping, so the
shipped artifact can be checked directly:

    grep -ac "some string you just added" ipks/<the>.ipk    # after extracting
    grep -ac "some string you just added" fbuild/webos-gl/quakehd.bin

The build tracks header, build-script and appinfo.json timestamps as well as
.c files, so editing a header does force a rebuild -- but it is still worth
verifying the artifact when a change mysteriously does nothing.


================================================================================
CONTROLS
================================================================================

Three input methods work at once: the on-screen touch overlay, a USB or
Bluetooth game controller, and a USB or Bluetooth keyboard. Plug a controller or
keyboard in at any time -- they are picked up within about a second, with no
need to restart the game.


--------------------------------------------------------------------------------
KEYBOARD
--------------------------------------------------------------------------------

These are Quake 1.09's own stock bindings. NOTE: there is no WASD -- movement is
on the arrow keys, which is how Quake shipped in 1996. See "Rebinding" below if
you want WASD.

  Movement
    Up Arrow / Down Arrow      Move forward / backward
    Left Arrow / Right Arrow   Turn left / right
    Alt (hold) + Left/Right    Strafe instead of turn
    , and .                    Strafe left / right
    Shift (hold)               Run
    Space or Enter             Jump
    D / C                      Swim up / down

  Combat
    Ctrl                       Fire
    1 - 8                      Select weapon directly
    /                          Next weapon

  Looking
    A / Z                      Look up / down
    Page Down / Delete         Look up / down
    End                        Centre the view
    Insert (hold)              Keyboard look
    \ (hold)                   Mouse look

  Interface
    Esc                        Menu
    ` (backtick)               Console
    Tab                        Scoreboard
    T                          Chat message
    Pause                      Pause
    - / =                      Shrink / enlarge the view
    F1                         Help
    F2 / F3                    Save / Load menu
    F4                         Options menu
    F5                         Multiplayer menu
    F6 / F9                    Quicksave / Quickload
    F10                        Quit
    F11                        Zoom in
    F12                        Screenshot

Every printable key, arrow, function key and modifier reaches the game, and
holding Shift produces capitals and shifted symbols when typing in the console.
Verified with a Mitsumi/Apple Extended USB keyboard; a Bluetooth keyboard
paired through webOS should work the same way, as it arrives over the same
/dev/input path.

Rebinding: open the console with ` and use Quake's normal bind command, for
example

    bind w +forward
    bind s +back
    bind a +moveleft
    bind d +moveright

Bindings are saved to id1/config.cfg and persist across launches.


--------------------------------------------------------------------------------
GAME CONTROLLER
--------------------------------------------------------------------------------

One scheme is used for every supported pad, so muscle memory carries between
them. Positions below are described physically; CONTROLS.md gives the exact
button for each pad, and CONTROLS.csv is the machine-readable table.

  In game
    Left stick, D-pad          Move and strafe
    Right stick                Look
    Face buttons               Look left / down / right / up
                               (so aiming works on pads with no right stick)
    Left shoulder              Jump
    Right shoulder             Fire
    Left trigger               Run (hold)
    Right trigger              Next weapon
    Stick clicks               Run
    Start                      Menu
    Back / Select              Previous weapon

  In menus
    D-pad, left stick          Navigate
    Right side of the pad      Select
      (face-down, face-right, right shoulder/trigger/stick-click)
    Left side of the pad       Back
      (face-left, face-up, left shoulder/trigger/stick-click, Back, Start)

No in-game action fires while a menu is open.

Verified pads: Sony DualShock 4 (USB and Bluetooth), ShanWan "Xbox knock-off"
wireless, DragonRise "Sega Saturn style" wireless, and the Logitech Precision
Gamepad. Other pads fall back to a generic profile that follows the standard
evdev button names and should work without configuration.


--------------------------------------------------------------------------------
TOUCH
--------------------------------------------------------------------------------

The touch overlay has four zones and only draws two of them, which is the single
biggest source of confusion. The two you cannot see are Jump and Menu -- read
their entries below before deciding either one is broken.

  Zone            Where                        Drawn?
  --------------  ---------------------------  ---------------------------------
  Move stick      Bottom-LEFT, 320x320 px      Yes, always
  Fire            Bottom-RIGHT, 320x320 px     Yes, always
  Jump            TOP strip, full width,       No -- see below
                  top 240 px of the screen
  Menu (Escape)   Two fingers anywhere in      No -- it is the leftover
                  the blank MIDDLE             space between the zones above

  Move            Press the on-screen stick and drag. It is analog: how far you
                  drag is how fast you move. There is a small dead zone at the
                  centre so resting your thumb does not creep.
  Fire            Tap or hold anywhere in the bottom-right zone. Holding
                  auto-fires.

  Jump            *** Slide, do not tap. ***
                  Put a finger anywhere along the top of the screen and MOVE it
                  slightly. A perfectly stationary tap does nothing at all.

  Menu            *** Two fingers, in the MIDDLE of the screen. ***
                  Sends Escape: opens the menu from a game or from the opening
                  demo, and backs out when a menu is already open. This is the
                  one gesture to remember -- it is the same everywhere.

QUIRK 1 -- the jump button is invisible, and it needs a slide.

  There is a jump graphic (images/jump.png), but it is not a button you press.
  It flashes at the top-centre of the screen for a few frames AFTER a successful
  jump, purely as confirmation. So the control you are looking for is not drawn
  anywhere: the whole top 240 px of the screen, edge to edge, is the jump zone.

  The zone only responds to finger MOVEMENT, not to a clean tap. Internally the
  jump fires from SDL_MOUSEMOTION, and a tap that never moves emits no motion
  event. Touch the top of the screen and slide a little and it jumps every time.

QUIRK 2 -- aim for the MIDDLE of the screen to open the menu, not the top.

  The natural instinct is to tap where the menu itself appears, which is high up
  under the Quake logo. That does not work reliably, and the reason is Quirk 1:
  the top 240 px is the jump strip, and fingers landing there are ignored by the
  menu gesture on purpose. Put both fingers around the middle of the panel --
  below the logo, above the on-screen stick -- and it opens first time.

  The safe area is everything except the three control zones, so the centre of
  the screen and the bottom-centre strip between the stick and fire both work.

  The gesture deliberately ignores fingers on the controls, because moving while
  firing is already two simultaneous contacts -- that is just normal play, and it
  must never pop the menu open mid-firefight.

  If you have used older builds, you may know a trick where double-tapping near
  the top of the title screen brought up the menu. That still works, but it was
  always an accident rather than a feature: the tap landed in the invisible jump
  zone, jump sends Space, and during the attract demo Quake opens the menu on any
  printable keypress (keys.c: `if (cls.demoplayback && down && consolekeys[key]
  && key_dest == key_game) M_ToggleMenu_f();`). It needed two taps because the
  first often did not move far enough to emit a motion event. It also only ever
  worked during the demo. Use the two-finger tap instead -- it works everywhere.

Once a menu IS open, the overlay is reinterpreted so a touch-only player can
navigate it:

  Fire zone (bottom-right)   Enter / Select
  Top strip                  Escape (back out, or close the menu)
  Move stick (bottom-left)   Arrow keys -- one step per push, re-arming when you
                             return the stick toward centre

Escape is also available on the other input methods, if you have them attached:

    Game controller   Start
    Keyboard          Escape

The overlay can be hidden entirely with orange + '@' on a hardware keyboard.
There is no touch gesture for this.

If the two-finger tap ever fails to register on some other device, set
`developer 1` (in id1/autoexec.cfg, say) and every touch contact is logged to
/media/internal/quakehd.log as:

    touch: contact 1 down at 600,420 -- 2 neutral down

The contact number is what matters. webOS reports each finger as its own SDL
"mouse" index via Palm's SDL_GetMultiMouseState extension, and the gesture needs
to see an index other than 0. If every line says "contact 0", the platform is
collapsing the fingers and the gesture cannot work as written.
